# LinkCpp-inspired PDFit shell

- Scope: shared dark theme, existing left navigation styling, responsive work-area spacing, and home empty-state composition.
- Reference: `F:\linkcpp_dev\apps\linker\src\front\components\base.css`, `frame.css`, `content.css`, and the supplied LinkCpp controller screenshot.
- Preserved: existing LNB items and interactions; no left-side tab strip was added.

## Validation

- `npm run build`: passed through `npm run deploy` (`@pdfit/pdfit` and `pdfit` builds succeeded).
- `npm run verify:repo`: passed.
- `npm run verify:pdfit`: passed.
- Docker: `pdfit-pdfit-1` healthy after the official `npm run deploy` path; `GET http://127.0.0.1:15201/health` returned `{"ok":true,"service":"pdfit"}`.
- Browser smoke: passed at `http://127.0.0.1:15201/`; 0 console errors and 0 page errors.
- Browser scenario: passed at `/folder/%EC%9D%B4%EB%B6%81`; main landmark and `PDF 목록` were present.
- Evidence: `test/20260805/20260805_linkcpp-theme-smoke/report.md`, `screenshots/smoke.png`, and `screenshots/folder-surface.png`.

## Known baseline

- The repository-wide i18n checker currently reports the pre-existing absence of `apps/<service>/assets/i18n`; this visual change added no new page-level literal copy beyond the existing Home content.
