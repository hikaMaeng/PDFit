# U-drive library rebuild

## Scope

The library volume was moved from the previous NAS-backed `linker-models`
volume to the new `pdfit-usbshare1` CIFS volume, backed by `U:\이북`. The
PostgreSQL data directory was reset as explicitly requested.

## Evidence

| Check | Result | Evidence |
| --- | --- | --- |
| U-drive source | passed | `U:\이북` contains 713 PDFs |
| Docker volume probe | passed | `pdfit-usbshare1` exposes 713 PDFs at `/library/이북` |
| DB reset | passed | previous `pgvector` moved to `pgvector-reset-backup-20260806_105728`; new tag count is 0 |
| API discovery | passed | `/api/folders/이북` reports `pdfCount: 713` |
| Health | passed | `http://127.0.0.1:15201/health` returned `{"ok":true,"service":"pdfit"}` |
| Docker | passed | `pdfit-pdfit-1` is healthy |
| Browser refresh | passed | `test/20260806/111000_u-drive-library-refresh/report.md` |

## Configuration

The ignored local `.env` now selects `PDFIT_BOOKS_VOLUME=pdfit-usbshare1` and
`PDFIT_BOOKS_SUBPATH=이북`. The tracked example and deploy probe use the same
configurable subpath instead of assuming `bside/이북`.
