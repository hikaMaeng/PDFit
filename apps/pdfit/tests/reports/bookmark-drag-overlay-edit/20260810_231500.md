# Test Report: bookmark-drag-overlay-edit

## Result

PASS.

## Commands

* `npm run test:pdfgpu` — passed (PDFit integration: 3/3).
* `npm run deploy` — passed; `pdfit-pdfit-1` healthy.
* `node test/20260810/bookmark-drag-overlay-edit.mjs` — passed.

## Browser evidence

* Page 28, drag preview: 300 × 300 px.
* POST then PATCH observed with no console/page errors.
* Overlay projection changed from 300 px to 330 px after zoom.
* Screenshots: `test/20260810/bookmark-drag-overlay-edit/screenshots/01-drag-preview.png`, `03-edit-modal.png`, `04-edited-overlay.png`, `08-final-comment-tooltip.png`.

## Runtime evidence

`GET /health` returned HTTP 200 with `{"ok":true,"service":"pdfit"}`.
