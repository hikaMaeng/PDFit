# PDFit 0.4.3 release report — 2026-08-11

## Result

PASS — PDFit `0.4.3` built, passed its release and integration checks, and was
deployed through the official Docker entrypoint before publication.

## Release scope

* Synchronized root, shared package, integrated app, Docker runtime package,
  and lockfile versions to `0.4.3`.
* Added immutable `release-contracts/pdfit/v0.4.3.json` while preserving the
  exact `@pdfgpu/core@0.1.9` source commit and tarball digest.
* Includes bidirectional bookmark synchronization and its topic-routed
  cross-window message transport.

## Validation

* `npm run verify:release-contract` — passed.
* `npm run build` — passed, 2/2 Turbo tasks.
* `npm run test:pdfgpu` — passed, 5/5.
* `npm run verify:repo` — passed.
* `npm run verify:pdfit` — passed against the deployed service.
* Package docs graph check with apps included — passed with 0 violations; the
  existing `src-dir-absent-from-table:common` warning remains.
* `git diff --check` — passed for the release change.

## Docker validation

* Command: `npm run deploy` — passed; host build completed before Compose refresh.
* Image: `sha256:bfbd319c328b75c40bd3e6499d80bf85bd64b4b4616ce72d97b3c89e5259d3a7`.
* Container: `pdfit-pdfit-1`, healthy, port `15201` published.
* `GET http://127.0.0.1:15201/health` — HTTP 200,
  `{"ok":true,"service":"pdfit"}`.
* `/app/package.json` reports version `0.4.3`; `/app/data/books` exposes 6
  top-level entries from the host bind.

## Browser evidence

* Scenario: `test/20260811/release-v0.4.3/run.mjs`.
* Input/final URL: `http://127.0.0.1:15201/`.
* Rendered sidebar version: `v0.4.3`.
* Console errors: 0; page errors: 0.
* JSON: `test/20260811/release-v0.4.3/report.json`.
* Screenshot: `test/20260811/release-v0.4.3/screenshots/release-root-v0.4.3.png`.
