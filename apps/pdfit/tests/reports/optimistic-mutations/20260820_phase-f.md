# Phase F - full verification and Docker redeploy

- Timestamp: 2026-08-20 (Asia/Seoul)
- Branch: `agent/align-pdfit-service-0.4.3`

## Official verification

- `npm run sync:pdfgpu`: pass
- `npm run verify:release-contract`: pass
- `npm run build`: pass (4/4 Turbo tasks)
- `npm run verify:repo`: pass
- `npm run test:service`: pass
- `npm run deploy`: pass; local index: 717 PDFs across 2 folders
- `npm run verify:pdfit`: pass
- `npm run verify:pdfit:acceptance`: pass 31/31 on isolated port 15211 and disposable data
- `npm run test:pdfgpu`: pass (5/5)
- `npm run deploy:service`: pass
- `npm run verify:service-parity`: pass (PDFit 0.4.3)

## Browser verification

- Local `http://127.0.0.1:15201/`: PDFit 0.4.3 rendered 713 + 4 PDFs; no browser console errors.
- Service `http://127.0.0.1:15202/`: PDFit 0.4.3 Google sign-in screen rendered; no browser console errors.
- Destructive acceptance ran only against an isolated Compose project and was removed after verification.

## Docker verification

- `pdfit-pdfit-1`: running, healthy.
- `pdfit-service-1`: running, healthy.
- `GET http://127.0.0.1:15201/health`: 200 `{"ok":true,"service":"pdfit"}`.
- `GET http://127.0.0.1:15202/health`: 200 `{"ok":true,"service":"pdfit-service"}`.
- Recent 10-minute logs: no `error`, `exception`, `fatal`, or `unhandled` matches.

## External verification boundary

- Account-authenticated Google Drive/Spreadsheet operations were not executed in this phase; the user elected to perform Google account verification.
