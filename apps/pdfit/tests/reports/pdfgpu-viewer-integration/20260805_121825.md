# Test Report: pdfgpu-viewer-integration

## Date

2026-08-05

## Result

PASS for the merged viewer integration and deployed Free runtime.

## Commands

* `npm run deploy` — passed; rebuilt and refreshed `pdfit-free` and `pdfit-pro`.
* `npm run test:pdfgpu` — passed; 9 tests, 0 failures.
* `npm run verify:repo` — passed.
* `npm run verify:free` — passed.
* `npm run verify:free:acceptance -- --group D` — passed; 8/8 cases.
* `node .codex/skills/headless-browser-test/scripts/run-headless-browser-test.mjs --url http://127.0.0.1:15201/viewer --out test/20260805/0318_free-viewer-smoke` — passed.

## Runtime Evidence

* `docker compose ps`: `pdfit-free` is healthy.
* `GET http://127.0.0.1:15201/health`: HTTP 200, `{"ok":true,"service":"pdfit-free"}`.
* Acceptance report: `apps/free/tests/reports/free-acceptance-matrix/20260805_031550.md`.
* Headless report: `test/20260805/0318_free-viewer-smoke/report.md`.

## Coverage Note

The headless Chromium environment reports `backend: unsupported`, so hardware WebGPU execution is not proven by this run. PDFium WASM loading, viewer controls, page movement, zoom/fit/invert, view modes, and the 2D fallback rendering path were exercised successfully. Hardware WebGPU validation remains a GPU-enabled browser/device test item.
