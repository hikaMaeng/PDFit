# Bookmark library navigation — 2026-08-11

## Result

PASS — the global bookmark entry is rendered between the folder and tag sections, and its library route shows the saved capture cards.

## Scope

- `GET /api/bookmarks` returns the cross-book bookmark library.
- `GET /bookmarks` provides Recent and By Book views.
- The LNB `BOOKMARKS` row is placed after `FOLDERS` and before `TAGS` and shows the current count.
- Bookmark image assets resolve from the current storage root and from the compatible legacy root.

## Validation

- `npm run build` — passed.
- `npm run test:pdfgpu` — passed (3/3).
- `npm run deploy` — passed; `pdfit-pdfit-1` became healthy on port 15201.
- `GET /health` — HTTP 200, `{"ok":true,"service":"pdfit"}`.
- `node test/20260811/bookmark-library-navigation.mjs` — passed:
  - order indices: folders 11, bookmarks 26, tags 36;
  - Recent card border: `rgb(20, 184, 166)`;
  - By Book cards: 5;
  - browser errors: none.

## Evidence

- `test/20260811/bookmark-library-navigation/screenshots/01-lnb-bookmarks.png`
- `test/20260811/bookmark-library-navigation/screenshots/02-bookmark-recent.png`
- `test/20260811/bookmark-library-navigation/screenshots/03-bookmark-by-book.png`
- `test/20260811/bookmark-library-navigation/report.json`
