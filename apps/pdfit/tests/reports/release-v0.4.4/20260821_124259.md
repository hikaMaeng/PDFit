# PDFit 0.4.4 release report — 2026-08-21

- Timestamp: 2026-08-21 12:42:59 KST
- Result: PASS

## Verification

| Check | Result |
|---|---|
| Workspace/package/runtime versions | PASS — all active package manifests report `0.4.4` |
| Exact internal dependency versions | PASS — `@pdfit/pdfit` and `@pdfit/service_domain` use `0.4.4` |
| `npm run verify:release-contract` | PASS |
| `npm run build` | PASS — 4/4 Turbo tasks |
| `npm run deploy` | PASS — Docker image rebuilt and container recreated |
| `npm run deploy:service` | PASS — service image rebuilt and container recreated |
| `npm run verify:pdfit` | PASS |
| Container package versions | PASS — both `pdfit` and `service` report `0.4.4` from `/app/package.json` |
| Container health | PASS — both containers healthy |
| Local browser version | PASS — `http://127.0.0.1:15201/` renders `v0.4.4` |
| Cloudflare browser version | PASS — the public service renders `v0.4.4` after the service redeploy |
| Local browser console | PASS — no errors |

The local bundle is `/assets/service-CRHfz_n6.js`; the service bundle is
`/assets/service-CEm9iGSh.js`.

At 12:46 KST, a browser comparison exposed that only the local `pdfit`
container had initially been redeployed. The Cloudflare tunnel still served
the previous `service` container and displayed `v0.4.3`. Running
`npm run deploy:service` rebuilt that second deployment, after which both
browser endpoints displayed `v0.4.4`.
