# Acceptance matrix after real-library-root cutover

## Result

The focused real-library-root E2E passed. The full legacy acceptance command
was started against the Docker-managed SMB volume but exceeded the 180-second
tool window while exercising the remote filesystem. Its two child Node
processes were identified by their exact acceptance command lines and stopped;
the service was then allowed to stabilize and returned healthy.

## Evidence

| Check | Result |
| --- | --- |
| `npm run deploy` | passed |
| Docker library volume | passed; `linker-models` exposes `S:\bside\이북` |
| Root API discovery | passed; `이북`, 88 PDFs, `isRoot: true` |
| Focused browser E2E | passed; create, root upload, move, delete file, delete folder |
| Full legacy acceptance matrix | timed out at 180 seconds on remote SMB I/O |
| Service recovery | passed; `pdfit` healthy, `/health` HTTP 200 |
| Container root | `/app/data/books/bside/이북` |
| PostgreSQL extensions | `textsearch_ko`, `vector` |
| Source cleanup | passed; no temporary test folder/file remained; root count 88 |

Focused browser evidence is recorded in
`test/20260805/163500_root-library-e2e/report.md`.
