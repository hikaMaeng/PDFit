# Bookmark card three-row layout — 2026-08-11

## Result

PASS — bookmark-library cards use a fixed 150 px image frame and three ordered
information rows: title/page, bookmark creation date-time, and comment.

## Scope

- `BookmarkPage` renders `BookmarkRecord.createdAt` with a Korean date-time
  formatter; no duplicate React state or API contract change was introduced.
- The comment row remains reserved when a bookmark has no comment, so cards in
  the same grid row keep identical dimensions.
- The image frame declares the same 150 px height as its height, minimum height,
  and maximum height.

## Validation

- `npm run build` — passed; the production `BookmarkPage` bundle contains the
  fixed image frame and three row test anchors.
- `npm run deploy` — passed through the repository deploy entrypoint.
- Docker — `pdfit-pdfit-1` healthy on published port `15201`.
- `GET /health` — HTTP 200, `{"ok":true,"service":"pdfit"}`.
- `node test/20260811/bookmark-card-three-row.mjs` — passed against
  `http://127.0.0.1:15201/bookmarks`:
  - persisted cards: 5;
  - image frame heights: 150 px;
  - first-row card heights: 219.59375 px;
  - every card exposed ordered title, creation date-time, and comment rows;
  - browser console errors: none;
  - browser page errors: none.

## Known repository-level checks

- The i18n checker still reports the pre-existing missing
  `apps/<service>/assets/i18n` directory. This change adds no new label or other
  user-visible literal; it formats bookmark timestamp data only.
- `npm run verify:pdfit` reached the running UI but timed out waiting for the
  currently absent `Settings` text. The failure is outside this card layout
  scenario and is not represented as passing.

## Evidence

- `test/20260811/bookmark-card-three-row/screenshots/01-bookmark-card-three-row.png`
- `test/20260811/bookmark-card-three-row/report.json`
