# Test Report: free-split-smoke
## Timestamp
2026-04-16 01:50:36 +09:00

## Environment
Windows PowerShell, `F:\dev\pdfit`, Docker Compose root file, Playwright Chromium, local `apps/free/.env`.

## Executed Steps
1. Ran `npm run build`.
2. Ran `npm run deploy`.
3. Ran `npm run verify:repo`.
4. Ran `npm run release:free:stage`.
5. Ran `npm run verify:free`.

## Observed Logs
`docker compose ps -a` showed `pdfit-pdfit-free-1` up on `15201`.

`docker compose logs --tail=50 pdfit-free` showed:

* SQLite experimental warning from Node 22
* `[pdfit-free] listening on http://localhost:15201`
* 404 response for the settings probe path during verification

## Failures
Initial deploy attempt failed because a legacy single-app container still owned port `15201`. The deploy scripts were updated to remove target containers and use `--remove-orphans`, then deploy succeeded.

## Reproducibility
Current workspace state reproduces the passing result with:

* `npm run deploy`
* `npm run verify:repo`
* `npm run release:free:stage`
* `npm run verify:free`
