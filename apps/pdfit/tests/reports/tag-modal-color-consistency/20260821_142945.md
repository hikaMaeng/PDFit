# 태그 관리 모달 색상 일치 검증 보고서

- 실행 시각: 2026-08-21 14:29:45 (Asia/Seoul)
- 환경: Windows 11, Docker Compose, PDFit v0.4.4, 로컬 포트 15201
- 대상: `/folder/이북` 첫 PDF의 태그 관리 모달

## 실행 결과

1. `npm run build`: 성공
2. `npm run deploy`: 성공
3. Docker 라이브러리 인덱스: 2개 폴더, PDF 717개
4. 브라우저 태그 관리 모달 표시: 성공
5. 실제 계산된 스타일:
   - `만화`: 글자/테두리/삭제 아이콘 `rgb(249, 115, 22)` (`#f97316`)
   - `미적분`: 글자/테두리/삭제 아이콘 `rgb(34, 197, 94)` (`#22c55e`)

## 판정

통과. 모달의 태그 칩이 각 태그에 저장된 색상과 일치한다.

## 실패 및 재현성

- 실패 없음
- 재현 절차: `/folder/이북`에서 `(만화)미적분 7일만에 끝내기.pdf`의 태그 관리 버튼을 누른 뒤 두 태그 칩의 색상을 목록 및 사이드바와 비교한다.
