# Test Report: sqlite-to-pgvector

## Result

PASS.

## Deployment

* `npm run deploy` passed.
* `pdfit-free` and `pdfit-pro` are healthy single-container services.
* `GET http://127.0.0.1:15201/health` returned HTTP 200.
* `GET http://127.0.0.1:15202/health` returned HTTP 200.

## Database Evidence

Free and Pro embedded PostgreSQL databases both reported `vector` and `textsearch_ko` extensions. Free contains `bookmarks`, `book_tags`, `reading_progress`, `tags`, and `viewer_state`; Pro contains those tables plus `ai_servers`, `ai_server_models`, and `pro_settings`.

The Free API probe wrote and read bookmark, progress, tag, and viewer-state records from PostgreSQL, confirmed one row in each table with `psql`, and removed the probe rows afterward.

## Verification

* `npm run verify:repo` — passed.
* `npm run verify:free` — passed.
* `npm run verify:free:acceptance -- --group D` — passed, 8/8.
* `npm run verify:pro` — passed.
* `npm run test:pdfgpu` — passed, 9/9.
* Free headless viewer smoke — passed: `test/20260805/1246_free-pgvector-viewer-smoke/report.md`.
* Pro headless viewer smoke — passed: `test/20260805/1246_pro-pgvector-viewer-smoke/report.md`.

## Reset Note

Free acceptance reset now truncates PostgreSQL tables inside the container instead of deleting the Windows bind-mounted PostgreSQL data directory. This avoids permission errors from files owned by the container's `postgres` user.

The workspace contained no legacy `app.db`, so no source migration was needed during this deployment. A startup compatibility path is included for installations that still have one.
