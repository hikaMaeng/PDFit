# PDFit 0.4.2 release report — 2026-08-11 15:05:45

## Result

PASS — PDFit `0.4.2` was built, committed locally, deployed through the
repository entrypoint, and rendered successfully from the healthy Docker
service. No commit was pushed.

## Release scope

- Synchronized the root, shared package, integrated app, Docker runtime
  package, and lockfile versions to `0.4.2`.
- Added `release-contracts/pdfit/v0.4.2.json` while preserving the exact
  `@pdfgpu/core@0.1.9` source commit and tarball digest.
- Released the bookmark new-window page-navigation fix from `ad3f469`.
- Release commit: `b44a62d chore(release): bump PDFit to v0.4.2`.

## Static and integrated validation

- `npm run verify:release-contract` — passed.
- `npm run build` — passed, 2/2 Turbo tasks.
- `npm run verify:repo` — passed.
- `npm run test:pdfgpu` — passed, 5/5 tests.
- `npm run verify:pdfit` against the deployed service — passed.
- Package docs graph check with apps included — passed with 0 violations; the
  existing `src-dir-absent-from-table:common` warning remains.
- `git diff --check` — passed for the release change.

## Docker validation

- Command: `npm run deploy`.
- Command result: exit 0 in 36.7 seconds; host build ran before Compose refresh.
- Image: `sha256:6f9ad90a121506cdd992f55b207ac45817f98ecf42ecde877c2eed728c1d1360`.
- Container: `pdfit-pdfit-1`, healthy with failing streak 0.
- Published port: `0.0.0.0:15201`.
- `GET http://127.0.0.1:15201/health` — HTTP 200,
  `{"ok":true,"service":"pdfit"}`.
- `/app/package.json` reports version `0.4.2`.
- `/app/data/books` is mounted and exposes 6 top-level entries.

The existing deploy entrypoint completed successfully but does not emit the
machine-readable `deploy-report-begin` block required by the deploy skill; the
runtime checks above were collected explicitly after deployment.

## Browser evidence

- Mode: headless Chromium scenario.
- Input URL: `http://127.0.0.1:15201`.
- Final URL: `http://127.0.0.1:15201/`.
- Result: passed; document status 200, title `PDFit`, main landmark present, and
  rendered sidebar text `v0.4.2` asserted.
- Console errors: 0; page errors: 0.
- Report: `test/20260811/150545_pdfit-v0.4.2-release/browser/report.md`.
- JSON: `test/20260811/150545_pdfit-v0.4.2-release/browser/report.json`.
- Screenshot: `test/20260811/150545_pdfit-v0.4.2-release/browser/screenshots/release-root-v0.4.2.png`.

## Known pre-existing architecture findings

The architecture guard reported one violation and one warning outside this
version-only change:

- `check name=pair:pdfit status=violation detail=missing-packages/pdfit_domain`
- `check name=wire-in-app-boundary status=warn detail=wire types under src/front or src/server; move to common/protocol`

The repository's documented ownership uses `packages/pdfit` as the shared
package, so this patch release does not rename or restructure it.
