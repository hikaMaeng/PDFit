# Test Report: viewer-center-grid-toggle

## Scope

- 화면을 3×3 그리드로 나눈 중앙 셀 클릭은 Space와 같은 툴바 표시 상태 전이를 사용한다.
- 중앙 셀 밖 클릭과 북마크 추가 모드의 중앙 클릭은 툴바 상태를 바꾸지 않는다.
- GPU 뷰어와 legacy fallback은 `PdfViewerPage`의 같은 세션 모델 경로를 사용한다.

## Automated validation

- `$env:PDFGPU_REFRESH='0'; npm run test:pdfgpu`: passed, 5/5 tests.
- `$env:PDFGPU_REFRESH='0'; npm run build`: passed.
- `npm run verify:repo`: passed.
- `npm run verify:pdfit`: passed after updating its icon-only Settings locator to role/name.
- `bash .codex/skills/i18n-resource-map/scripts/check-i18n.sh`: existing baseline violation; `apps/<service>/assets/i18n` is absent. This change adds no user-visible string.

## Docker validation

- Deploy command: `$env:PDFGPU_REFRESH='0'; npm run deploy`.
- Container: `pdfit-pdfit-1`, healthy.
- Published port: `0.0.0.0:15201->15201/tcp`.
- `GET http://127.0.0.1:15201/health`: `200`, `{ "ok": true, "service": "pdfit" }`.
- `/app/data/books`: visible in the container; six top-level entries observed.

## Browser scenario

- Mode: scenario-based headless Chromium.
- Target: the deployed 472-page PDF under `/viewer/이북/1. 이벤트소싱과 마이크로서비스 아키텍처 472p.pdf`.
- Result: passed.
- Assertions:
  - central-cell outside click keeps the toolbar visible;
  - bookmark capture mode ignores a central click;
  - normal central click hides the toolbar;
  - Space restores the toolbar;
  - console errors: 0;
  - page errors: 0.
- Report: `test/20260811/center-grid-toolbar-toggle/report.md`.
- Screenshots: `test/20260811/center-grid-toolbar-toggle/screenshots/01-toolbar-visible.png` through `04-space-toolbar-restored.png`.

## Acceptance runner note

The isolated F03 runner on port 15202 stopped during fixture setup because the existing upload path returned 200 but did not refresh PostgreSQL book metadata, so `waitForFile` could not observe the seeded PDF. It did not reach this feature's assertions. The disposable acceptance container used a dedicated Compose file and data directory and was removed after the run. The deployed read-only live-PDF scenario above covers the requested interaction and the bookmark-mode exception directly.
