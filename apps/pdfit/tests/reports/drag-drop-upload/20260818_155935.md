# Test Report: PDF drag-and-drop upload

## Timestamp

2026-08-18 15:59:35 KST

## Environment

- Windows 11
- PDFit 0.4.3
- Docker Compose service `pdfit`
- Chromium via Playwright
- URL: `http://127.0.0.1:15201`

## Steps and results

1. Built `@pdfit/pdfit` and both production applications: passed.
2. Rebuilt and recreated the `pdfit` Docker image: passed.
3. Restored host port publishing with the dedicated `pdfit-host` network: passed.
4. Dragged two generated PDFs through a browser `DataTransfer`: passed.
5. Confirmed the drop overlay, both filenames, and `PDF 2개`: passed.
6. Deleted the uniquely named temporary test folder: passed.
7. Ran `npm run verify:repo`: passed.
8. Ran `npm run verify:pdfit`: passed.
9. Ran `npm run verify:service-parity`: passed.
10. Inspected the deployed `이북` folder in the in-app browser: passed.

## Logs

```text
drag-drop upload passed: drag-one.pdf, drag-two.pdf
verify:repo passed
verify:pdfit passed
service parity passed: PDFit 0.4.3
pdfit-pdfit-1 healthy, 0.0.0.0:15201->15201/tcp
```

## Limitations

- The hosted `service` image was built with the shared drag-and-drop UI, but
  its local container was stopped because the required Google OAuth client ID
  and secret are not configured in the root `.env`.
- Google OAuth itself was not tested.

## Reproduction

```powershell
npm run verify:pdfit:drag-drop
npm run verify:pdfit
npm run verify:repo
```
