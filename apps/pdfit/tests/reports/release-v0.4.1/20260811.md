# PDFit 0.4.1 release report — 2026-08-11

## Result

PASS — PDFit `0.4.1` was built, verified, pushed to `origin/master`, deployed
through the repository entrypoint, and rendered successfully from the healthy
Docker service.

## Release scope

- Synchronized the root, shared package, integrated app, Docker runtime
  package, and lockfile versions to `0.4.1`.
- Added `release-contracts/pdfit/v0.4.1.json` while preserving the exact
  `@pdfgpu/core@0.1.9` source commit and tarball digest.
- Included the two commits after `v0.4.0`: bookmark-card three-row alignment and
  the center-grid viewer toolbar toggle.
- Release commit: `d01fbfe chore(release): bump PDFit to v0.4.1`.

## Static and integrated validation

- `npm run verify:release-contract` — passed.
- `npm run build` with `PDFGPU_REFRESH=0` — passed, 2/2 Turbo tasks.
- `npm run verify:repo` — passed.
- `npm run verify:pdfit` against the deployed service — passed.
- Package docs graph check with apps included — passed with 0 violations; the
  existing `src-dir-absent-from-table:common` warning remains.
- `git diff --check` — passed for the release change.

## Docker validation

- Command: `npm run deploy` with `PDFGPU_REFRESH=0`.
- Command result: exit 0 in 35.1 seconds; host build ran before Compose refresh.
- Image: `sha256:2e97befb0763db63b7b1c9dfd22c6204a439614bb98717857cfaefe03eaa7fd1`.
- Container: `pdfit-pdfit-1`, `Up (healthy)`.
- Published port: `0.0.0.0:15201->15201/tcp` and `[::]:15201->15201/tcp`.
- `GET http://127.0.0.1:15201/health` — HTTP 200,
  `{"ok":true,"service":"pdfit"}`.
- `/app/package.json` reports version `0.4.1`.
- `/app/data/books` is visible in the container with 6 top-level entries.

The existing deploy entrypoint completed successfully but does not emit the
machine-readable `deploy-report-begin` block required by the deploy skill; the
runtime checks above were collected explicitly after deployment.

## Browser evidence

- Mode: headless Chromium smoke.
- Input URL: `http://127.0.0.1:15201`.
- Final URL: `http://127.0.0.1:15201/`.
- Result: passed; document status 200, title `PDFit`, main landmark present.
- The rendered sidebar displays `v0.4.1`.
- Console errors: 0; page errors: 0.
- Report: `test/20260811/142652_headless-browser-test/report.md`.
- JSON: `test/20260811/142652_headless-browser-test/report.json`.
- Screenshot: `test/20260811/142652_headless-browser-test/screenshots/smoke.png`.

## Known pre-existing architecture findings

The architecture guard reported one violation and one warning outside this
version-only change:

- `check name=pair:pdfit status=violation detail=missing-packages/pdfit_domain`
- `check name=wire-in-app-boundary status=warn detail=wire types under src/front or src/server; move to common/protocol`

The repository's documented ownership uses `packages/pdfit` as the shared
package, so this release does not rename or restructure it.
