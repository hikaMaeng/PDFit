# Test Report: free-entry-split-refactor
## Timestamp
2026-04-16 02:57:09 +09:00

## Environment
* Workspace: `F:\dev\pdfit`
* OS: Windows
* Shell: PowerShell
* Browser automation: Playwright Chromium headless
* Containers: `pdfit-free`, `pdfit-pro` via root `docker-compose.yml`

## Executed Steps
1. Ran `npm run build`.
2. Ran `npm run deploy`.
3. Ran `npm run verify:free`.
4. Ran `npm run verify:repo`.
5. Confirmed `apps/free/dist/index.html` and `apps/free/dist/viewer/index.html` exist.
6. Confirmed `apps/free/dist/server/routes/settings.js` is absent.

## Observed Logs
* `npm run build`: workspace build passed; Free Vite output included `assets/service-DUOYR-HA.js` and `assets/viewer-BGziulKn.js`.
* `npm run deploy`: `docker compose rm -sf pdfit-free pdfit-pro` completed, then `docker compose up -d --build --force-recreate --remove-orphans pdfit-free pdfit-pro` started both services.
* `docker compose ps pdfit-free pdfit-pro`:
  `pdfit-pdfit-free-1` up on `0.0.0.0:15201->15201/tcp`
  `pdfit-pdfit-pro-1` up on `0.0.0.0:15202->15202/tcp`
* `docker compose logs --tail=20 pdfit-free` included:
  `[pdfit-free] listening on http://localhost:15201`
  `[pdfit-free] WARN GET /settings/ai-servers 404 (1ms)`
* `npm run verify:free`: passed.
* Bundle checks from `scripts/verify-free.mjs` confirmed:
  manifest entries for `index.html` and `viewer/index.html`
  no `/api/settings` or `AI servers` string in the Free service bundle
  no `Create folder` or `/api/settings` string in the Free viewer bundle
* Browser checks from `scripts/verify-free.mjs` confirmed:
  `/` loaded `PDFit Free`
  Free UI exposed no `Settings`
  `/viewer` loaded `PDF Viewer`
  Free viewer exposed no `Home` navigation

## Failures
* None.

## Reproducibility
* Re-run `npm run deploy`, then `npm run verify:free` and `npm run verify:repo` from `F:\dev\pdfit`.
