# Agent Guardrails

Determine the integrated app from these identifiers:

| Check | Contract |
| --- | --- |
| App path | `apps/pdfit` |
| Dist path | `apps/pdfit/dist` |
| Compose service | `pdfit` |
| Dockerfile | `apps/pdfit/docker/Dockerfile` |
| Published port | `15201` |

Never recreate the old Free/Pro app split, add a second public service, or modify `C:\Users\hika0\pdfit-release` as part of this workspace task.

Required completion evidence is a committed change, root and app docs, a test plan/report, the official deploy command, Docker health, and browser verification where UI behavior changed.
